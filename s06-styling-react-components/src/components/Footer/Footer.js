import myStyles from './Footer.module.css'

const Footer = () => {
    return (
        <footer className={myStyles.footer}>
            {myStyles.footer}
        </footer>
    )
}

export default Footer